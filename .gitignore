# Python
.venv/
*.egg-info/
__pycache__/
*.pyc

# Data (large, not tracked; `data` is a symlink to an external volume)
data
.pytest_cache
.claude

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# Project instructions (per-machine)
CLAUDE.md

# Coverage
coverage.xml
htmlcov/
.coverage

# Jupyter
.ipynb_checkpoints/
# Papermill / executed outputs (regenerable, often multi-MB)
*-executed.ipynb

# ClickHouse server droppings (guard against accidental `clickhouse server` launch from repo root)
cores/
metadata/
metadata_dropped/
store/
flags/
format_schemas/
user_files/
preprocessed_configs/
access/
uuid
status
tmp/
